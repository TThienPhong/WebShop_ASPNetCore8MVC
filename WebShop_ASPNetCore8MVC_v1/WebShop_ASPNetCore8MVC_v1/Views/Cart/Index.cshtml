@model IEnumerable<WebShop_ASPNetCore8MVC_v1.ViewModels.CartItem>
@{
    ViewData["Title"] = "Index";
}

<!-- Single Page Header start -->
<div class="container-fluid page-header py-5">
    <h1 class="text-center text-white display-6">Cart</h1>
    <ol class="breadcrumb justify-content-center mb-0">
        <li class="breadcrumb-item"><a href="/">Home</a></li>
        <li class="breadcrumb-item"><a href="#">Pages</a></li>
        <li class="breadcrumb-item active text-white">Cart</li>
    </ol>
</div>
<!-- Single Page Header End -->
<!-- Cart Page Start -->
<div class="container-fluid py-5">
    <div class="container py-5">
        <div class="table-responsive">
            <table class="table">
                <thead>
                    <tr>
                        <th scope="col">Products</th>
                        <th scope="col">Name</th>
                        <th scope="col">Price</th>
                        <th scope="col">Quantity</th>
                        <th scope="col">Total</th>
                        <th scope="col">Handle</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach(var item in Model)
                    {
                        <tr>
                            <th scope="row">
                                <div class="d-flex align-items-center">
                                    <a asp-controller="HangHoa" asp-action="Detail" asp-route-id="@item.MaHh">
                                        <img src="Hinh/HangHoa/@item.Hinh" class="img-fluid me-5 rounded-circle" style="width: 80px; height: 80px;" alt="@item.TenHH">
                                    </a>
                                    
                                </div>
                            </th>
                            <td>
                                <a asp-controller="HangHoa" asp-action="Detail" asp-route-id="@item.MaHh">
                                    <p class="mb-0 mt-4">@item.TenHH</p>
                                </a>
                                
                            </td>
                            <td>
                                <p class="mb-0 mt-4">@item.DonGia</p>
                            </td>
                            <td>
                                <div class="input-group quantity mt-4" style="width: 100px;">
                                   @*  <form class="input-group quantity" asp-controller="Cart" asp-action="UpdateQuantity" asp-route-id="@item.MaHh" method="get">
                                        <div class="input-group-btn">
                                            <button class="btn btn-sm btn-minus rounded-circle bg-light border">
                                                <i class="fa fa-minus"></i>
                                            </button>
                                        </div>
                                        <input type="text" class="form-control form-control-sm text-center border-0" value="@item.SoLuong" name="quantity" >
                                        <div class="input-group-btn">
                                            <button class="btn btn-sm btn-plus rounded-circle bg-light border">
                                                <i class="fa fa-plus"></i>
                                            </button>
                                        </div>
                                    
                                    </form> *@
                                    <div class="input-group-btn">
                                        <button class="btn btn-sm btn-minus rounded-circle bg-light border">
                                            <i class="fa fa-minus"></i>
                                        </button>
                                    </div>
                                    <input type="text" class="form-control form-control-sm text-center border-0 quantity-input" value="@item.SoLuong" min="1" data-item-id="@item.MaHh">
                                    <div class="input-group-btn">
                                        <button class="btn btn-sm btn-plus rounded-circle bg-light border">
                                            <i class="fa fa-plus"></i>
                                        </button>
                                    </div>

                                    
                                </div>
                            </td>
                            <td>
                                <p class="mb-0 mt-4"> @item.ThanhTien</p>
                            </td>
                            <td>
                                <a asp-controller="Cart" asp-action="RemoveCartItem" asp-route-id="@item.MaHh" class="btn btn-md rounded-circle bg-light border mt-4">
                                    <i class="fa fa-times text-danger"></i>
                                </a>
                            </td>

                        </tr>

                      

                       
                    }

                   
                </tbody>
            </table>
        </div>
        <div class="mt-5">
            <input type="text" class="border-0 border-bottom rounded me-5 py-3 mb-4" placeholder="Coupon Code">
            <button class="btn border-secondary rounded-pill px-4 py-3 text-primary" type="button">Apply Coupon</button>
        </div>
        <div class="row g-4 justify-content-end">
            <div class="col-8"></div>
            <div class="col-sm-8 col-md-7 col-lg-6 col-xl-4">
                <div class="bg-light rounded">
                    <div class="p-4">
                        <h1 class="display-6 mb-4">Cart <span class="fw-normal">Total</span></h1>
                        <div class="d-flex justify-content-between mb-4">
                            <h5 class="mb-0 me-4">Subtotal:</h5>
                            <p class="mb-0">@Model.Sum(p => p.ThanhTien)</p>
                        </div>
                        <div class="d-flex justify-content-between">
                            <h5 class="mb-0 me-4">Shipping</h5>
                            <div class="">
                                <p class="mb-0">Flat rate: $3.00</p>
                            </div>
                        </div>
                        <p class="mb-0 text-end">Shipping to Ukraine.</p>
                    </div>
                    <div class="py-4 mb-4 border-top border-bottom d-flex justify-content-between">
                        <h5 class="mb-0 ps-4 me-4">Total</h5>
                        <p class="mb-0 pe-4">@Model.Sum(p => p.ThanhTien)</p>
                    </div>
                    <a asp-controller="Cart" asp-action="Checkout" class="btn border-secondary rounded-pill px-4 py-3 text-primary text-uppercase mb-4 ms-4" type="button">Thanh toán</a>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- Cart Page End -->
@section Scripts{
    <script>
        console.log("Gia trị cart");
        $('.quantity-input').change(function () {
            var quantity = $(this).val();
            var id = $(this).data('item-id');
            console.log("Gia trị cart quantity", quantity);
            console.log("Gia trị cart id", id);
            let url = `/Cart/UpdateQuantity?id=` + id + `&quantity=` + quantity;
            window.location.href = url;

        @*  // Gửi dữ liệu đến controller
            $.ajax({
            url: '/Cart/UpdateQuantity', // Thay đổi đường dẫn đến controller và action tương ứng
            type: 'POST',
            data: { id: id, quantity: quantity },
            success: function(response) {
            // Xử lý phản hồi từ controller nếu cần
            consolo.log("ajax");
            },
            error: function(xhr, status, error) {
            // Xử lý lỗi nếu có
            console.error(xhr.responseText);
            }

            }); *@
        });

    </script>
   
}

  
@*  không chay được *@